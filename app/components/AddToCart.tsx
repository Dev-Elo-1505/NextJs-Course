"use client";
import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button className="btn glass" onClick={() => console.log("Click")}>
        Add to cart
      </button>
    </div>
  );
};

export default AddToCart;
